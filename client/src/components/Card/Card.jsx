import React from 'react';
import { Container, Heading } from './Card.style';

const Card = (props) => {
  return (
    <Container>
      <Heading>Card Works</Heading>
    </Container>
  );
};

export default Card;
